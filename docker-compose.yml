version: '3.8'
services:
  kitchen-service:
    build:
      context: ./microservices/kitchen-service
      dockerfile: Dockerfile
    ports:
      - "8080:8080" # Map port 8080 of the container to port 8080 on the host
    environment:
      - NODE_ENV=${NODE_ENV}

  # warehouse-service:
  #   build:
  #     context: ./microservices/warehouse-service
  #     dockerfile: Dockerfile
  #   ports:
  #     - "8081:8080" # Assuming warehouse service also runs on port 8080 within the container, map it to 8081 on the host
  #   environment:
  #     - NODE_ENV=${NODE_ENV}
  #     - DATABASE_URL=${DATABASE_URL}

  # order-management-ui:
  #   build:
  #     context: ./order-management-ui
  #     dockerfile: Dockerfile # Assuming you have a Dockerfile here as well
  #   ports:
  #     - "3000:3000" # Adjust the ports as necessary for your UI service
  #   environment:
  #     - NODE_ENV=production
